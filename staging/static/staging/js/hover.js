function showOnHover(hoverId, showId) {
    const hoverElement = document.getElementById(hoverId);
    const showElement = document.getElementById(showId);
    
    hoverElement.addEventListener('mouseenter', function() {
      showElement.style.display = 'block';
    });
    
    hoverElement.addEventListener(  'mouseleave', function() {
      showElement.style.display = 'none';
    });
  }

  // Call the function with the IDs of the elements you want to interact with
  showOnHover('hoverElement2', 'hiddenElement2');
  function showOnHover(hoverId, showId) {
    const hoverElement = document.getElementById(hoverId2);
    const showElement = document.getElementById(showId2);
    
    hoverElement.addEventListener('mouseenter', function() {
      showElement.style.display = 'block';
    });
    
    hoverElement.addEventListener(  'mouseleave', function() {
      showElement.style.display = 'none';
    });
  }

  // Call the function with the IDs of the elements you want to interact with
  showOnHover('hoverElement3', 'hiddenElement3');
  function showOnHover(hoverId, showId) {
    const hoverElement = document.getElementById(hoverId3);
    const showElement = document.getElementById(showId3);
    
    hoverElement.addEventListener('mouseenter', function() {
      showElement.style.display = 'block';
    });
    
    hoverElement.addEventListener(  'mouseleave', function() {
      showElement.style.display = 'none';
    });
  }

  // Call the function with the IDs of the elements you want to interact with
  showOnHover('hoverElement4', 'hiddenElement4');
  function showOnHover(hoverId, showId) {
    const hoverElement = document.getElementById(hoverId4);
    const showElement = document.getElementById(showId4);
    
    hoverElement.addEventListener('mouseenter', function() {
      showElement.style.display = 'block';
    });
    
    hoverElement.addEventListener(  'mouseleave', function() {
      showElement.style.display = 'none';
    });
  }

  // Call the function with the IDs of the elements you want to interact with
  showOnHover('hoverElement5', 'hiddenElement5');
  function showOnHover(hoverId, showId) {
    const hoverElement = document.getElementById(hoverId);
    const showElement = document.getElementById(showId);
    
    hoverElement.addEventListener('mouseenter', function() {
      showElement.style.display = 'block';
    });
    
    hoverElement.addEventListener(  'mouseleave', function() {
      showElement.style.display = 'none';
    });
  }

  // Call the function with the IDs of the elements you want to interact with
  showOnHover('hoverElement', 'hiddenElement');